```{r data generation, echo = FALSE, results = "hide"}
library(exams)
library(MASS)
data(forbes)
```

Question
===============

Dopo aver caricato in memoria la libreria `MASS`, si acceda al dataset `forbes` contenuto in tale libreria. 

1. (1pt) Si acceda alla documentazione del dataset `forbes` e si descriva il contenuto del dataset: cosa rappresentano le variabili contenute in tale insieme di dati?

1. (2pt) Si ottengano le medie campionarie e le mediane per le variabili `bp` e `pres`. Si ottengano quindi il primo ed il terzo quartile. Sono presenti outlier nelle due distribuzioni?

1. (1pt) Si ottengano gli istogrammi di tali variabili, scegliendo opportunamente il numero di intervalli. 

1. (1pt) Si ottenga un grafico che metta in relazione le variabili `bp` e `pres`.

1. (1pt) Si calcoli la correlazione tra le variabili `bp` e `pres`.

1. (2pt) In un modello di regressione lineare semplice $y = \alpha + \beta x$, dove `bp` rappresenta la variabile esplicativa ($x$) e `pres` rappresenta la variabile risposta ($y$), le stime ai minimi quadrati sono pari a
$$
\hat{\alpha} = \bar{y} - \hat{\beta}\bar{x}, \qquad \hat{\beta} = \frac{\text{cov}(x,y)}{\text{var}(x)}.
$$
Si ottengano quindi i valori di $\hat{\alpha}$ e $\hat{\beta}$ usando **R**. 

1. (2pt) Si rappresenti la retta di regressione ottenuta congiuntamenti ai dati. Il modello sembra adattarsi bene ai dati?


Meta-information
================
extype: string
exsolution: nil
exname: regression essay
exextra[essay,logical]: TRUE
exextra[essay_format,character]: editor
exextra[essay_required,logical]: TRUE
exextra[essay_fieldlines,numeric]: 15
exextra[essay_attachments,numeric]: 1
exextra[essay_attachmentsrequired,logical]: TRUE
